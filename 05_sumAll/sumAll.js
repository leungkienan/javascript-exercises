const sumAll = function(min, max) {
    if()
        var n = (max - min + 1);
        var sum = n * (max + min) / 2; // note integer division
    
        return sum;
};

// Do not edit below this line
module.exports = sumAll;
